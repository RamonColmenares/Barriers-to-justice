# Optimized requirements for smaller deployment size
Flask==3.0.0
flask-cors==4.0.0

# Use lighter data processing libraries where possible
pandas==2.2.0
numpy==1.26.3

# For plotting - plotly is still needed but ensure it's minimal
plotly==5.18.0

# Only include scipy if absolutely necessary for stats
scipy==1.14.1

# For HTTP requests
requests==2.31.0

# Add these optimizations to reduce bundle size
# wheel  # Uncomment if needed for faster installs
